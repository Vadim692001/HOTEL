@model IEnumerable<IdentityHotel.Models.Number>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Komplektaci_nomera)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kilcist_pipl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Komplektaci_nomera)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kilcist_pipl)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Number }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Number }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Number })
        </td>
    </tr>
}

</table>
